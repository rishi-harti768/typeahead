---
"typeahead": patch
---
Add durable search submission path including a memory aggregation Write Buffer, sequential disk log (WAL) appending via Bun's fast API, background Flush-and-Truncate periodic task, startup crash recovery on boot, and extended /metrics analytics endpoints.
